import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fahadmukasa.workforcemanagement',
  appName: 'Lot2545 Workforce',
  webDir: 'dist'
};

export default config;
